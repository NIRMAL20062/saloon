package com.glide.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Fixed, light-only palettes — one per role, per docs/THEME_AND_ROLES_COLOR_GUIDE.md.
 * GLIDE has no dark mode. Never add a dark variant of either object below.
 */
object CustomerColors {
    val Background = Color(0xFFF6F1E8)
    val Surface = Color(0xFFFFFFFF)
    val HairlineBorder = Color(0xFFE5DDD3)
    val Ink = Color(0xFF201A1D)
    val MutedText = Color(0xFF756C70)
    val Accent = Color(0xFF512A45)
    val AccentSurface = Color(0xFFF3EBF0)
    val StarRating = Color(0xFFE7C45A)
    val LiveOpen = Color(0xFF23845B)
    val UrgentCoral = Color(0xFFF06F61)
}

object PartnerColors {
    val Background = Color(0xFFF8FAFC)
    val CardSurface = Color(0xFFFFFFFF)
    val CardBorder = Color(0xFFE2E8F0)
    val PrimaryBrand = Color(0xFF0D7A53)
    val MintSurface = Color(0xFFEBF5F0)
    val MintBorder = Color(0xFFD1FAE5)
    val TextPrimary = Color(0xFF111827)
    val TextMuted = Color(0xFF64748B)
    val PendingAlert = Color(0xFFF59E0B)
    val Danger = Color(0xFFEF4444)
    val DangerSurface = Color(0xFFFEF2F2)
}
